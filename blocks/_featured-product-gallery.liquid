

{% liquid
  assign product = closest.product
%}

{% capture children %}
  {% unless product == blank %}
    {% if settings.quick_add or settings.mobile_quick_add %}
      {% render 'quick-add', product: product, section_id: section.id %}
    {% endif %}
  {% endunless %}
{% endcapture %}

{% render 'card-gallery', children: children %}

{% stylesheet %}
  .featured-product-section .card-gallery .quick-add__button {
    position: absolute;
    left: var(--quick-add-offset, 0.75rem);
    right: var(--quick-add-offset, 0.75rem);
    bottom: var(--quick-add-offset, 0.75rem);
    width: calc(100% - (var(--quick-add-offset, 0.75rem) * 2));
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:names.product_media",
  "tag": null,
  "settings": [
    {
      "type": "paragraph",
      "content": "t:content.resource_reference_product_media"
    },
    {
      "type": "select",
      "id": "image_ratio",
      "label": "t:settings.aspect_ratio",
      "options": [
        {
          "value": "adapt",
          "label": "t:options.auto"
        },
        {
          "value": "portrait",
          "label": "t:options.portrait"
        },
        {
          "value": "square",
          "label": "t:options.square"
        },
        {
          "value": "landscape",
          "label": "t:options.landscape"
        }
      ],
      "default": "adapt"
    },
    {
      "type": "select",
      "id": "constrain_to_viewport",
      "options": [
        {
          "value": "",
          "label": "t:options.off"
        },
        {
          "value": "contain",
          "label": "t:options.maintain_aspect_ratio"
        },
        {
          "value": "cover",
          "label": "t:options.crop_to_fit"
        }
      ],
      "default": "contain",
      "label": "t:settings.limit_media_to_screen_height",
      "visible_if": "{{ block.settings.image_ratio == 'adapt' }}"
    }
  ]
}
{% endschema %}
